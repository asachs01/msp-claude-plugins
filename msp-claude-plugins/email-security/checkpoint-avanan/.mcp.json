{
  "mcpServers": {
    "checkpoint-avanan": {
      "type": "http",
      "url": "${CHECKPOINT_AVANAN_MCP_URL}",
      "headers": {
        "X-Checkpoint-Client-Id": "${CHECKPOINT_CLIENT_ID}",
        "X-Checkpoint-Client-Secret": "${CHECKPOINT_CLIENT_SECRET}"
      }
    }
  }
}
