{
  "mcpServers": {
    "proofpoint": {
      "type": "http",
      "url": "${PROOFPOINT_MCP_URL}",
      "headers": {
        "X-Proofpoint-Principal": "${PROOFPOINT_SERVICE_PRINCIPAL}",
        "X-Proofpoint-Secret": "${PROOFPOINT_SERVICE_SECRET}"
      }
    }
  }
}
