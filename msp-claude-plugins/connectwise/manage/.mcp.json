{
  "mcpServers": {
    "connectwise-manage": {
      "command": "npx",
      "args": ["-y", "@msp-plugins/connectwise-manage-mcp-server"],
      "env": {
        "CW_COMPANY_ID": "${CW_COMPANY_ID}",
        "CW_PUBLIC_KEY": "${CW_PUBLIC_KEY}",
        "CW_PRIVATE_KEY": "${CW_PRIVATE_KEY}",
        "CW_CLIENT_ID": "${CW_CLIENT_ID}",
        "CW_BASE_URL": "${CW_BASE_URL}"
      }
    }
  }
}
