{
  "mcpServers": {
    "ninjaone": {
      "type": "http",
      "url": "${NINJAONE_MCP_URL}",
      "headers": {
        "X-Ninja-Client-ID": "${NINJAONE_CLIENT_ID}",
        "X-Ninja-Client-Secret": "${NINJAONE_CLIENT_SECRET}",
        "X-Ninja-Region": "${NINJAONE_REGION}"
      }
    }
  }
}
