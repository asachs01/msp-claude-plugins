{
  "mcpServers": {
    "autotask": {
      "type": "http",
      "url": "${AUTOTASK_MCP_URL}",
      "headers": {
        "X-API-Key": "${AUTOTASK_USERNAME}",
        "X-API-Secret": "${AUTOTASK_SECRET}",
        "X-Integration-Code": "${AUTOTASK_INTEGRATION_CODE}"
      }
    }
  }
}
