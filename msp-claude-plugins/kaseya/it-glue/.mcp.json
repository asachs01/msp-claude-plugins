{
  "mcpServers": {
    "it-glue": {
      "command": "npx",
      "args": ["-y", "it-glue-mcp"],
      "env": {
        "IT_GLUE_API_KEY": "${IT_GLUE_API_KEY}",
        "IT_GLUE_REGION": "${IT_GLUE_REGION:-us}"
      }
    }
  }
}
