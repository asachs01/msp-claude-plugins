{
  "mcpServers": {
    "itglue": {
      "type": "http",
      "url": "${ITGLUE_MCP_URL}",
      "headers": {
        "X-ITGlue-API-Key": "${ITGLUE_API_KEY}",
        "X-ITGlue-Region": "${ITGLUE_REGION}"
      }
    }
  }
}
