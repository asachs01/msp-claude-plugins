{
  "mcpServers": {
    "datto-rmm": {
      "type": "http",
      "url": "${DATTO_RMM_MCP_URL}",
      "headers": {
        "X-Datto-API-Key": "${DATTO_API_KEY}",
        "X-Datto-API-Secret": "${DATTO_API_SECRET}",
        "X-Datto-Platform": "${DATTO_PLATFORM}"
      }
    }
  }
}
