{
  "mcpServers": {
    "superops": {
      "type": "http",
      "url": "${SUPEROPS_MCP_URL}",
      "headers": {
        "X-SuperOps-Token": "${SUPEROPS_API_TOKEN}",
        "X-SuperOps-Subdomain": "${SUPEROPS_SUBDOMAIN}"
      }
    }
  }
}
